 # Spring Boot console application can schedule activities

 Activity selection based on day input
 Factory Pattern

## Run
 ```bash
./mvnw spring-boot:run
 ```

## Docker

```bash
docker build -t scheduler .
docker run -it scheduler
 ```
